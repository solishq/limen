[package]
name = "limen_foundation_ops"
description = "Four concrete foundation operations: Refusal, Authority+Commit, Governance, Cascade."
edition.workspace = true
version.workspace = true
license.workspace = true

# MUST NOT depend on: limen_projection, limen_chain, any AI/ML crate, any product crate (v1.3 §0.2)
[dependencies]
limen_types = { workspace = true }
limen_foundation_contract = { workspace = true }

[dev-dependencies]
limen_foundation_contract = { workspace = true, features = ["test-support"] }
