/**
 * Type declaration for @langchain/langgraph-checkpoint peer dependency.
 * Provides BaseCheckpointSaver base class type.
 */
declare module '@langchain/langgraph-checkpoint' {
  export abstract class BaseCheckpointSaver {
    constructor();
  }
}
